---
title: UI 擴展
description: 新增 HotCRM 視圖、頁面、儀表板和動作。
---

# UI 擴展

HotCRM UI 中繼資料位於 `src/views/`、`src/pages/`、`src/dashboards/`、`src/reports/` 和 `src/actions/`。

## 新增動作

```ts
// src/actions/opportunity.actions.ts
import type { Action } from '@objectstack/spec/ui';

export const MarkOpportunityReviewedAction: Action = {
  name: 'mark_opportunity_reviewed',
  label: 'Mark Reviewed',
  objectName: 'crm_opportunity',
  icon: 'check-circle',
  type: 'modal',
  target: 'mark_opportunity_reviewed',
  locations: ['record_header'],
  body: {
    language: 'js',
    capabilities: ['api.write'],
    source: `
      await ctx.api.object('crm_opportunity').update(input.id, {
        next_step: 'Reviewed by manager'
      });
      return { id: input.id };
    `,
  },
  refreshAfter: true,
};
```

## 新增視圖和頁面

- 清單和看板定義放在 `src/views/`。
- 記錄頁面和工具頁面放在 `src/pages/`。
- 從對應的 `index.ts` 匯出新中繼資料。
- 將使用者可見標籤加入 `src/translations/{locale}.ts`。

## 驗證

```bash
pnpm validate
pnpm typecheck
pnpm test
```
