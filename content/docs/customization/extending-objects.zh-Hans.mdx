---
title: 扩展对象
description: 添加或修改 HotCRM 对象、字段、hooks、流程和共享元数据。
---

# 扩展对象

HotCRM 对象位于拥有它的那个包的 `objects/` 目录下——`src/sales/`、`src/service/`、`src/revenue/` 或 `src/marketing/`——每个包各自从该目录的 `index.ts` 注册自己的对象。`src/` 下没有顶层的 `objects/` 目录：`src/` 下的一个目录**就是**一个包（ADR-0130）。下面这个 warranty 放进 revenue 包，与 `crm_product`、`crm_contract` 这些它紧挨着的对象为邻；你自己的对象放在它所属领域的那个包里。

## 新增对象

```ts
// src/revenue/objects/warranty.object.ts
import { ObjectSchema, Field } from '@objectstack/spec/data';

export const Warranty = ObjectSchema.create({
  name: 'crm_warranty',
  label: 'Warranty',
  pluralLabel: 'Warranties',
  icon: 'shield',
  fields: {
    warranty_number: Field.autonumber({ label: 'Warranty Number', format: 'WR-{000000}' }),
    crm_account: Field.lookup('crm_account', { label: 'Account', required: true }),
    crm_product: Field.lookup('crm_product', { label: 'Product' }),
    status: Field.select({
      label: 'Status',
      defaultValue: 'active',
      options: [
        { label: 'Active', value: 'active', default: true },
        { label: 'Expired', value: 'expired' },
      ],
    }),
  },
  enable: {
    apiEnabled: true,
    searchable: true,
    trackHistory: true,
  },
});
```

然后导出：

```ts
// src/revenue/objects/index.ts
export { Warranty } from './warranty.object.js';
```

## 新增生命周期逻辑

`*.hook.ts` 与它命名的那个 `*.object.ts` 并排放在同一个包里，并从该包自己的 `objects/hooks.ts` 注册——这里是 `src/revenue/objects/hooks.ts`——再由 `objectstack.composition.ts` 汇入 `allHooks`。两处都没到达的 hook 会被静默忽略：构建照样是绿的，而处理函数永远不会运行。

```ts
// src/revenue/objects/warranty.hook.ts
import type { Hook } from '@objectstack/spec/data';

const warrantyHook: Hook = {
  name: 'crm_warranty_hook',
  object: 'crm_warranty',
  events: ['beforeInsert', 'beforeUpdate'],
  handler: async (ctx) => {
    const doc = ctx.input.doc as Record<string, unknown>;
    if (doc.status === 'expired' && !doc.end_date) {
      throw new Error('Expired warranties need an end date.');
    }
  },
};

export default warrantyHook;
```

## 新增自动化

多步骤自动化写在其触发对象所属包的 `flows/` 目录下——`src/sales/flows/`、`src/service/flows/`、`src/revenue/flows/` 或 `src/marketing/flows/`。warranty 提醒由 `crm_warranty` 触发，所以它和上面那个对象一样归 revenue 包。

```ts
// src/revenue/flows/warranty-alert.flow.ts
import type * as Automation from '@objectstack/spec/automation';

type Flow = Automation.Flow;

export const WarrantyAlertFlow: Flow = {
  name: 'warranty_alert',
  label: 'Warranty Alert',
  type: 'record_change',
  status: 'active',
  variables: [],
  nodes: [
    { id: 'start', type: 'start', label: 'Warranty updated', config: { objectName: 'crm_warranty', triggerType: 'record-after-update' } },
    { id: 'end', type: 'end', label: 'End' },
  ],
  edges: [{ id: 'e1', source: 'start', target: 'end', type: 'default' }],
};
```

注册分两步，漏掉第二步会让这条流程被静默丢弃。先从该包的 barrel 重新导出，再把它加入
`objectstack.composition.ts` 里的 `allFlows` —— 正是这个数组、而不是那个 barrel，被
`objectstack.config.ts` 交给 `defineStack({ flows })`：

```ts
// src/revenue/flows/index.ts —— 包自己的 barrel：只做重新导出，别无其它
export { WarrantyAlertFlow } from './warranty-alert.flow.js';
```

```ts
// objectstack.composition.ts —— 全应用的那份清单，按流程注册顺序排列
import { WarrantyAlertFlow } from './src/revenue/flows/index.js';

export const allFlows = [
  // …已有的流程
  WarrantyAlertFlow,
];
```

只进 barrel 是不够的。`allFlows` 之所以是一份显式有序清单，是因为注册顺序在四个包之间交错，
任何按包切分的 barrel 都承载不了它。一条到了 barrel 却没进这个数组的流程，注册它的是零：
`pnpm validate` 照样退出 0，而且哪里都不会提到它——能发现它的是 `pnpm test`，套件会点名任何
被包导出、却不在 `allFlows` 里的流程。

## 验证

```bash
pnpm validate
pnpm typecheck
pnpm test
```
