import type { Dashboard } from '@objectstack/spec/ui';

const SalesDashboard: Dashboard = {
  name: 'sales_dashboard',
  label: '销售主管仪表盘',
  widgets: [
    // ObjectUI Definition to be implemented
  ]
};

export default SalesDashboard;
