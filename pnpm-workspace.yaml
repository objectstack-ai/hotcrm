packages:
  - 'apps/*'
  - 'packages/*'
  # server is an internal-only package (private: true) excluded from
  # workspace linking to avoid circular dependency issues during builds.
  # It is consumed via tsconfig path aliases instead.
  - '!packages/server'
