# HotCRM 深度开发计划

> **最后更新**: 2026-01-29  
> **协议版本**: @objectstack/spec v0.6.1  
> **架构**: 元数据驱动、TypeScript原生、基于ObjectQL

## 📋 执行摘要

本文档概述了将HotCRM从当前基础推进到世界级企业CRM系统的综合开发计划。该计划遵循@objectstack/spec v0.6.1协议规范，并强调：

1. **TypeScript优先架构**：将所有遗留YAML定义迁移到类型安全的TypeScript
2. **业务逻辑扩展**：实现全面的钩子和自动化
3. **AI增强**：在所有主要功能中扩展AI能力
4. **高级CRM功能**：CPQ（配置-定价-报价）、服务管理和营销自动化
5. **企业就绪**：SLA、审批工作流、多租户

## 🎯 当前状态评估

### ✅ 已实现（TypeScript对象）

| 对象 | 状态 | 钩子 | 动作 | 优先级 |
|------|------|------|------|--------|
| **Account（客户）** | ✅ 完成 | ❌ 无 | ⚠️ 基础 | P0 |
| **Contact（联系人）** | ✅ 完成 | ❌ 无 | ❌ 无 | P1 |
| **Lead（线索）** | ✅ 完成 | ✅ 评分+状态 | ❌ 无 | P0 |
| **Opportunity（商机）** | ✅ 完成 | ✅ 阶段变更 | ❌ 无 | P0 |
| **Contract（合同）** | ✅ 完成 | ❌ 无 | ❌ 无 | P1 |

**现有钩子：**
- ✅ `lead.hook.ts`：线索评分、数据完整度、公海池管理
- ✅ `opportunity.hook.ts`：阶段变更自动化、合同创建

**现有动作：**
- ✅ `ai_smart_briefing.action.ts`：AI驱动的客户洞察

### ⚠️ 遗留（YAML对象 - 需要迁移）

| 对象 | 包 | 优先级 | 复杂度 | 预计工时 |
|------|-----|--------|--------|---------|
| **Campaign（营销活动）** | @hotcrm/crm | P1 | 中等 | 8小时 |
| **Activity（活动）** | @hotcrm/crm | P0 | 高 | 12小时 |
| **Product（产品）** | @hotcrm/products | P1 | 中等 | 8小时 |
| **Pricebook（价格手册）** | @hotcrm/products | P1 | 中等 | 6小时 |
| **Quote（报价）** | @hotcrm/products | P0 | 高 | 16小时 |
| **Case（服务工单）** | @hotcrm/support | P0 | 高 | 12小时 |
| **Knowledge（知识库）** | @hotcrm/support | P1 | 中等 | 10小时 |
| **Payment（付款）** | @hotcrm/finance | P1 | 中等 | 8小时 |

**总迁移工作量**：约80小时

### ❌ 尚未实现

仍缺失的关键对象：
- **CampaignMember（营销活动成员）**（营销）
- **Task（任务）**（活动管理）
- **Event（事件）**（日历）
- **Note（备注）**（文档）
- **Attachment（附件）**（文件管理）
- **User（用户）**（身份与访问）
- **Role（角色）**（RBAC）
- **Permission（权限）**（安全）

## 🗺️ 开发路线图

### 第一阶段：基础增强（第1-4周）- 优先级最高

**目标**：完成核心CRM对象和关键业务逻辑

#### 1.1 关键对象迁移（第1-2周）

**Activity对象迁移** ⭐ 最高优先级
- 迁移 `Activity.object.yml` → `activity.object.ts`
- 实现全面的活动跟踪：
  - 类型：电话、邮件、会议、任务、备注、短信
  - 状态跟踪及完成日期
  - 优先级管理
  - 相关对象关联（Who/What模式）
  - 重复任务支持
- 添加验证规则：
  - 基于活动类型的必填字段
  - 日期/时间约束
  - 分配验证
- 创建活动视图：
  - 我的活动（今天、本周、逾期）
  - 团队日历
  - 活动时间线

**Quote对象迁移** ⭐ 高优先级
- 迁移 `Quote.object.yml` → `quote.object.ts`
- 实现CPQ（配置-定价-报价）基础：
  - 带产品引用的报价行项目
  - 折扣管理（行级和报价级）
  - 税金计算框架
  - 多币种支持
  - 报价版本管理
  - 报价模板
- 添加审批工作流钩子：
  - 折扣审批阈值
  - 经理审批路由
  - 审批历史跟踪
- 创建报价视图：
  - 草稿报价
  - 待审批
  - 已发送给客户
  - 已接受/已拒绝

**Case对象迁移** ⭐ 高优先级
- 迁移 `Case.object.yml` → `case.object.ts`
- 实现服务管理：
  - 多渠道工单创建（邮件、电话、Web、聊天、微信）
  - SLA管理与自动计算
  - 升级规则和路由
  - 优先级和严重程度级别
  - 工单状态工作流
  - 客户满意度(CSAT)跟踪
- 添加自动化钩子：
  - 基于技能/可用性的自动分配
  - SLA违规通知
  - 自动升级
  - 客户通知
- 创建工单视图：
  - 我的工单
  - 团队队列
  - 已升级工单
  - SLA违规中
  - 今日已关闭

#### 1.2 业务逻辑实现（第2-3周）

**Account钩子** (`account.hook.ts`)
```typescript
// 关键自动化场景：
1. 健康度评分计算
   - 基于：活动频率、付款历史、支持工单、合同价值
   - 触发：每日批处理 + 重大事件发生时
   - 范围：0-100，带颜色编码

2. 层次结构管理
   - 自动更新上级客户指标
   - 级联所有权变更
   - 汇总下级客户价值

3. 客户状态自动化
   - 首次签约时自动将潜在客户升级为客户
   - 合同到期时自动标记为流失
   - 基于健康度评分标记风险客户

4. 合同价值汇总
   - 汇总所有活跃合同价值
   - 合同变更时更新
   - 如果是上级客户则包含下级客户

5. 续约日期管理
   - 从活跃合同中找到最近的续约日期
   - 在续约前90/60/30天设置提醒任务
```

**Contact钩子** (`contact.hook.ts`)
```typescript
// 关键自动化场景：
1. 最后联系日期跟踪
   - 任何与此联系人的活动发生时更新
   - 触发：活动创建/更新后

2. 决策链验证
   - 如果客户没有决策者则发出警告
   - 基于职位/级别自动设置影响力级别
   - 跟踪每个客户的多个决策者

3. 重复检测
   - 跨客户检查邮箱唯一性
   - 发现重复时建议合并
   - 按姓名+公司标记潜在重复项

4. 关系强度自动更新
   - 基于：活动频率、邮件情感、交易参与度
   - 自动提升 弱 → 中等 → 强
   - 90天以上无联系时自动降级
```

**Campaign钩子** (`campaign.hook.ts`)
```typescript
// 关键自动化场景：
1. ROI计算
   - 公式：(活动收入 - 成本) / 成本 * 100
   - 跟踪：创建的商机、生成的线索、赢得的收入
   - 相关商机/线索变更时自动更新

2. 预算跟踪
   - 支出超过预算80%时警告
   - 超过预算时阻止活动
   - 跟踪实际支出vs计划支出

3. 营销活动成员管理
   - 基于条件自动添加线索/联系人
   - 跟踪参与度：已发送、已打开、已点击、已回复
   - 移除退订成员

4. 营销活动效果
   - 转化率：线索 → 商机 → 成交
   - 按渠道的响应率
   - 每线索/商机/客户成本
```

**Activity钩子** (`activity.hook.ts`)
```typescript
// 关键自动化场景：
1. 到期日自动完成
   - 将逾期任务的状态设为已完成
   - 发送完成通知
   - 记录到时间线

2. 相关对象更新
   - 更新 Account.LastActivityDate
   - 更新 Contact.LastContactDate
   - 更新 Opportunity.LastActivityDate
   - 增加活动计数器

3. 逾期通知
   - 每日作业：查找逾期任务
   - 通知负责人和经理
   - 必要时创建跟进任务

4. 重复管理
   - 完成时自动创建下一次发生
   - 支持：每日、每周、每月模式
   - 结束日期或发生次数限制
```

#### 1.3 数据模型增强（第3-4周）

**新增支持对象**

**CampaignMember对象** (`campaign_member.object.ts`)
```typescript
// 多对多关系：Campaign ↔ Lead/Contact
字段：
- CampaignId（查找 → Campaign）
- LeadId（查找 → Lead，可选）
- ContactId（查找 → Contact，可选）
- Status：已发送、已打开、已点击、已回复、已退订
- FirstRespondedDate
- Notes
- MemberSource：手动、导入、API、自动化

验证：
- 必须有LeadId或ContactId之一
- 不能同时拥有两者
- 状态进展逻辑
```

**Task对象** (`task.object.ts`)
```typescript
// 任务管理的专用Activity类型
扩展：Activity
额外字段：
- Priority：高、普通、低
- Status：未开始、进行中、已完成、等待、延期
- DueDate（必填）
- ReminderDateTime
- RecurrenceType：每日、每周、每月、每年
- RecurrenceInterval
- RecurrenceEndDate
- SubTasks：复杂工作流的相关任务

功能：
- 看板视图
- 任务依赖
- 时间跟踪
- 检查清单
```

**Note对象** (`note.object.ts`)
```typescript
// 附加到任何对象的快速备注
字段：
- Title（255）
- Body（富文本，32000）
- ParentId（多态查找）
- ParentType：Account、Contact、Opportunity、Case等
- IsPrivate：布尔值
- Tags：多选或文本

功能：
- Markdown支持
- @提及以通知团队成员
- 固定重要备注
- 全文搜索
```

### 第二阶段：AI增强（第5-8周）

**目标**：使HotCRM成为真正的AI原生系统，具有智能自动化

#### 2.1 AI动作扩展

**Lead AI增强** (`lead_ai.action.ts`)
```typescript
1. 邮件签名数据提取
   - 解析来信中的联系信息
   - 提取：姓名、职位、公司、电话、邮箱、地址
   - 自动填充线索字段
   - 每个字段的置信度评分

2. 从网络丰富线索信息
   - 通过域名查找公司
   - 社交媒体资料发现
   - 行业分类
   - 员工数量估算
   - 收入估算

3. 智能线索路由
   - ML模型匹配最佳销售代表
   - 基于：行业专长、地理位置、工作负载、成交率
   - 考虑：线索评分、产品兴趣、交易规模
   - 团队间负载均衡

4. 线索培育建议
   - 建议下一步最佳行动
   - 邮件模板推荐
   - 最佳联系时间预测
   - 基于行业的内容推荐
```

**Opportunity AI增强** (`opportunity_ai.action.ts`)
```typescript
1. 赢单概率预测
   - 基于历史数据训练的ML模型
   - 特征：阶段、年龄、金额、竞争对手、活动、联系人级别
   - 变更时实时更新
   - 解释预测因素

2. 交易风险评估
   - 识别风险因素：
     * 停滞（超过14天无活动）
     * 竞争对手威胁
     * 预算担忧
     * 决策者未参与
   - 风险评分（0-100）
   - 推荐缓解措施

3. 下一步建议
   - 上下文感知建议：
     * 安排演示
     * 发送案例研究
     * 安排高管会议
     * 申请预算批准
   - 基于：当前阶段、交易特征、成功模式

4. 竞争情报
   - 识别备注/邮件中提到的竞争对手
   - 提取竞争对手要点
   - 建议差异化因素
   - 按竞争对手的赢/输分析

5. 最佳成交日期预测
   - 预测实际成交日期
   - 基于：历史销售周期、交易规模、行业
   - 标记过于乐观的预测
   - 自动调整预测类别
```

**Case AI增强** (`case_ai.action.ts`)
```typescript
1. 自动分类
   - ML分类工单类型
   - 提取：产品、功能、问题类型
   - 分配优先级/严重程度
   - 路由到正确队列

2. 智能分配
   - 匹配具有以下条件的客服：
     * 正确的技能/认证
     * 当前可用性/工作负载
     * 类似工单的历史成功率
     * 语言匹配
   - 负载均衡

3. 知识库RAG（检索增强生成）
   - 所有知识库文章的向量嵌入
   - 相似工单的语义搜索
   - 向客服自动建议知识库文章
   - 向客户自动建议（自助服务）
   - 生成草稿回复

4. SLA违规预测
   - 预测工单是否会违反SLA
   - 基于：工单复杂度、客服工作负载、时间
   - 主动升级
   - 重新分配给可用客服

5. 情感分析
   - 分析客户邮件/聊天
   - 检测：愤怒、沮丧、中立、满意
   - 标记负面情绪供经理审查
   - 跟踪每个客户的情绪趋势
```

**Campaign AI增强** (`campaign_ai.action.ts`)
```typescript
1. 内容生成
   - 生成邮件主题行（A/B测试就绪）
   - 从要点撰写邮件正文
   - 创建社交媒体帖子
   - 生成着陆页文案
   - 保持品牌声音

2. 受众细分
   - 基于ML的客户聚类
   - 为营销活动建议目标受众
   - 相似受众生成
   - 参与倾向评分

3. 发送时间优化
   - 预测每个接收者的最佳发送时间
   - 基于：历史打开情况、时区、行业
   - 列表的批量优化

4. 渠道推荐
   - 建议：邮件、社交、活动、直邮
   - 基于：受众资料、营销活动目标、预算
   - 每个渠道的预期ROI
```

#### 2.2 AI仪表板和洞察

**销售智能仪表板**
```typescript
组件：
1. 交易健康热图
   - 按风险级别颜色编码
   - 深入查看风险因素
   - 推荐行动

2. 管道预测与AI
   - AI调整后的预测 vs 销售代表预测
   - 置信区间
   - 假设场景分析

3. 重点关注的顶级商机
   - 排名依据：赢单概率 × 交易规模 × 成交时间
   - 每个商机的下一步最佳行动

4. 团队绩效分析
   - 成交率趋势
   - 销售周期分析
   - 活动级别 vs 结果
   - 辅导建议

5. AI警报和提醒
   - "交易停滞 - 10天无活动"
   - "高价值客户30天未联系"
   - "45天后合同续约"
   - "本周3笔交易中提到新竞争对手"
```

### 第三阶段：高级CRM功能（第9-14周）

#### 3.1 CPQ（配置-定价-报价）- 第9-11周

**产品配置引擎**
```typescript
1. 产品目录增强
   - 产品捆绑和套餐
   - 产品依赖关系（需要X才能添加Y）
   - 产品约束（X排除Y）
   - 配置选项和变体
   - 产品推荐（经常一起购买）

2. 定价规则引擎
   - 按数量的分层定价
   - 批量折扣
   - 基于合同的定价
   - 客户特定定价
   - 有日期范围的促销定价
   - 竞争定价分析

3. 折扣管理
   - 折扣审批工作流
   - 按金额/百分比的审批矩阵
   - 多级审批
   - 折扣原因代码
   - 折扣分析和报告
   - 利润保护规则

4. 报价生成
   - 专业PDF生成
   - 可自定义报价模板
   - 多语言支持
   - 数字签名集成
   - 报价到期跟踪
   - 待处理报价自动跟进

5. 报价到现金工作流
   - 报价 → 订单 → 合同 → 发票
   - 从接受的报价自动创建合同
   - 付款条款管理
   - 发票集成
```

**要创建的CPQ对象：**
- `product_bundle.object.ts`
- `price_rule.object.ts`
- `quote_line_item.object.ts`
- `approval_request.object.ts`
- `discount_schedule.object.ts`

**要创建的CPQ钩子：**
- `quote.hook.ts`：定价计算、审批路由
- `product.hook.ts`：捆绑验证、库存更新
- `pricebook.hook.ts`：生效日期管理

#### 3.2 服务管理 - 第11-12周

**工单管理增强**
```typescript
1. 全渠道工单创建
   - 邮件到工单自动化
   - Web到工单表单
   - 电话集成（CTI）
   - 聊天机器人集成
   - 社交媒体监听
   - 微信/WhatsApp集成

2. SLA管理
   - 按层级/类型的SLA模板
   - 工作时间日历
   - 升级规则
   - SLA里程碑跟踪：
     * 首次响应
     * 每次响应
     * 解决
   - SLA违规通知
   - SLA绩效报告

3. 工单路由和分配
   - 轮询分配
   - 基于技能的路由
   - 负载均衡
   - 基于区域的路由
   - VIP客户优先级路由
   - 溢出队列管理

4. 客户自助服务门户
   - 提交和跟踪工单
   - 搜索知识库
   - 社区论坛
   - 下载资源
   - 工单状态通知
```

**知识库增强**
```typescript
1. 文章管理
   - 富文本编辑器
   - 版本控制
   - 文章模板
   - 多语言支持
   - 文章分类和标签
   - 相关文章链接

2. 文章工作流
   - 草稿 → 审查 → 已发布 → 已归档
   - 审批流程
   - 计划发布
   - 过时内容自动归档

3. 文章分析
   - 查看次数
   - 有用/无用投票
   - 搜索分析
   - 工单解决中的使用情况
   - 热门文章仪表板

4. AI驱动功能
   - 自动分类
   - 自动标记
   - 文章摘要
   - 相关文章建议
   - 从文章回答问题
   - 客户支持的RAG
```

**要创建的服务对象：**
- `sla_policy.object.ts`
- `sla_milestone.object.ts`
- `escalation_rule.object.ts`
- `routing_rule.object.ts`
- `case_comment.object.ts`

**要创建的服务钩子：**
- `case.hook.ts`：SLA计算、路由、升级
- `knowledge.hook.ts`：文章评分、推荐

#### 3.3 营销自动化 - 第12-14周

**营销活动执行**
```typescript
1. 营销活动构建器
   - 可视化营销活动流程设计器
   - 拖放式邮件构建器
   - 着陆页构建器
   - 表单构建器
   - A/B测试框架

2. 邮件营销
   - 邮件模板库
   - 个性化令牌
   - 动态内容块
   - 发送时间优化
   - 可传递性监控
   - 退信/退订管理

3. 线索培育
   - 滴灌式营销活动
   - 触发式营销活动：
     * 欢迎系列
     * 放弃购物车
     * 购买后
     * 重新参与
   - 线索评分集成
   - 自动线索分配

4. 营销分析
   - 营销活动绩效仪表板
   - 邮件分析：打开率、点击率、转化率
   - 归因建模
   - ROI跟踪
   - 渠道绩效比较
```

**要创建的营销对象：**
- `email_template.object.ts`
- `landing_page.object.ts`
- `form.object.ts`
- `marketing_list.object.ts`
- `unsubscribe.object.ts`

**要创建的营销钩子：**
- `campaign.hook.ts`：ROI计算、绩效跟踪
- `campaign_member.hook.ts`：参与度跟踪、评分

### 第四阶段：企业功能（第15-18周）

#### 4.1 工作流和审批引擎

**工作流规则**
```typescript
1. 可视化工作流构建器
   - 触发条件
   - 操作：
     * 字段更新
     * 邮件警报
     * 任务创建
     * 出站消息
     * 自定义操作
   - 基于时间的工作流
   - 工作流版本控制

2. 审批流程
   - 多步骤审批
   - 按条件的审批矩阵
   - 并行审批
   - 委托审批
   - 自动审批规则
   - 审批历史跟踪

3. 流程自动化
   - 记录触发的流程
   - 计划流程
   - 平台事件
   - 错误处理
   - 流程监控
```

**要创建的对象：**
- `workflow_rule.object.ts`
- `workflow_action.object.ts`
- `approval_process.object.ts`
- `approval_step.object.ts`

#### 4.2 安全和访问控制

**RBAC（基于角色的访问控制）**
```typescript
1. 用户管理
   - 用户配置文件
   - 活跃/非活跃状态
   - 密码策略
   - 双因素认证支持
   - 单点登录（SSO）
   - 会话管理

2. 角色层次结构
   - 组织层次结构
   - 角色继承
   - 使用层次结构授予访问权限
   - 基于角色的记录访问

3. 权限
   - 对象级权限（CRUD）
   - 字段级安全性
   - 记录类型权限
   - 功能许可证

4. 记录共享
   - 组织范围的默认设置
   - 共享规则
   - 手动共享
   - 团队共享
   - 基于代码的共享
```

**要创建的对象：**
- `user.object.ts`
- `role.object.ts`
- `permission_set.object.ts`
- `sharing_rule.object.ts`

#### 4.3 数据管理

**数据质量**
```typescript
1. 重复检测
   - 按对象的匹配规则
   - 模糊匹配算法
   - 自动去重
   - 手动审查的合并UI
   - 防止创建重复项

2. 数据验证
   - 必填字段强制执行
   - 格式验证
   - 跨字段验证
   - 自定义验证规则
   - 验证规则测试

3. 数据导入/导出
   - 带映射的CSV导入
   - 大数据集的批量API
   - 数据模板
   - 导入错误处理
   - 计划导出
   - 数据备份/恢复
```

#### 4.4 报告和分析

**报告引擎**
```typescript
1. 报告构建器
   - 表格报告
   - 摘要报告
   - 矩阵报告
   - 连接报告
   - 报告图表
   - 报告过滤器

2. 仪表板
   - 仪表板组件：
     * 图表（条形图、折线图、饼图、圆环图）
     * 表格
     * 指标
     * 仪表
   - 动态仪表板（以用户身份运行）
   - 仪表板过滤器
   - 深入查看功能
   - 导出为PDF/Excel

3. 分析
   - 自定义公式
   - 计算字段
   - 跨对象公式
   - 汇总摘要
   - 趋势/历史数据
   - 预测
```

## 🔧 技术实现指南

### TypeScript对象迁移检查清单

从YAML迁移对象到TypeScript时：

```typescript
// ✅ 应该做：
1. 从@objectstack/spec导入类型定义
   import type { ServiceObject } from '@objectstack/spec/data';

2. 使用const和普通对象（无类型注释）
   const MyObject = {
     name: 'MyObject',
     // ... 字段
   };

3. 默认导出
   export default MyObject;

4. 遵循字段命名：字段名使用PascalCase
   Name, AccountId, CreatedDate

5. 精确使用协议字段类型
   type: 'text' | 'number' | 'currency' | 'date' | 'datetime' | 
         'select' | 'multiselect' | 'checkbox' | 'lookup' | 
         'masterDetail' | 'email' | 'phone' | 'url' | 'textarea' | 
         'percent' | 'geolocation' | 'richtext'

6. 定义relationships数组
   relationships: [
     {
       name: 'Contacts',
       type: 'hasMany',
       object: 'Contact',
       foreignKey: 'AccountId'
     }
   ]

7. 添加带过滤器的listViews
   listViews: [
     {
       name: 'AllRecords',
       label: '所有记录',
       filters: [],
       columns: ['Name', 'Status', 'OwnerId']
     }
   ]

8. 包含validationRules
   validationRules: [
     {
       name: 'RuleName',
       errorMessage: '用户友好的消息',
       formula: 'AND(Field1 > 0, ISBLANK(Field2))'
     }
   ]

9. 设计pageLayout
   pageLayout: {
     sections: [
       {
         label: '部分名称',
         columns: 2,
         fields: ['Field1', 'Field2']
       }
     ]
   }

// ❌ 不应该做：
1. 不再使用YAML格式
2. 不向对象添加类型注释
   const MyObject: ServiceObject = { // ❌ 否 
3. 不对字段名使用snake_case
   customer_name // ❌ 否 → CustomerName ✅
4. 不跳过必需的元数据（label、description）
```

### 钩子实现检查清单

```typescript
// 钩子模板
import type { Hook } from '@objectstack/spec/data';
import { db } from '@hotcrm/core';

interface TriggerContext {
  old?: Record<string, any>;
  new: Record<string, any>;
  db: typeof db;
  user: { id: string; name: string; email: string };
}

const MyObjectHook: Hook = {
  name: 'MyObjectHook',
  object: 'MyObject',
  events: ['beforeInsert', 'beforeUpdate', 'afterInsert', 'afterUpdate', 'beforeDelete', 'afterDelete'],
  handler: async (ctx: TriggerContext) => {
    try {
      // 验证
      if (!ctx.new) {
        console.warn('⚠️ 在没有上下文的情况下调用触发器');
        return;
      }

      // 这里是业务逻辑
      
    } catch (error) {
      console.error('❌ MyObjectHook中的错误:', error);
      throw error; // 或优雅地处理
    }
  }
};

export default MyObjectHook;

// ✅ 最佳实践：
1. 始终检查ctx.old和ctx.new
2. 使用try-catch进行错误处理
3. 记录重要操作
4. 使用db.doc.create/update/delete进行CRUD
5. 使用db.find进行查询
6. 保持钩子专注（单一职责）
7. 考虑性能（避免N+1查询）
8. 尽可能使钩子幂等
9. 在注释中记录钩子行为
10. 测试边缘情况（空值、缺失字段）
```

### 动作实现检查清单

```typescript
// 动作模板
import { db } from '@hotcrm/core';

export interface MyActionRequest {
  // 带JSDoc的请求参数
  recordId: string;
  options?: Record<string, any>;
}

export interface MyActionResponse {
  // 带JSDoc的响应结构
  success: boolean;
  data?: any;
  error?: string;
}

export async function executeMyAction(
  request: MyActionRequest
): Promise<MyActionResponse> {
  try {
    // 1. 验证输入
    if (!request.recordId) {
      throw new Error('recordId是必需的');
    }

    // 2. 获取数据
    const record = await db.doc.get('MyObject', request.recordId);
    if (!record) {
      throw new Error('未找到记录');
    }

    // 3. 业务逻辑
    // ...

    // 4. 返回结果
    return {
      success: true,
      data: result
    };

  } catch (error) {
    console.error('❌ MyAction中的错误:', error);
    return {
      success: false,
      error: error instanceof Error ? error.message : '未知错误'
    };
  }
}

export default executeMyAction;

// ✅ 最佳实践：
1. 为请求/响应使用TypeScript接口
2. 验证所有输入
3. 优雅地处理错误
4. 返回一致的响应格式
5. 为外部API调用添加重试逻辑
6. 记录重要操作
7. 考虑为昂贵操作进行缓存
8. 使动作无状态
9. 使用JSDoc注释记录
10. 编写单元测试
```

## 📊 成功指标

### 第一阶段成功标准
- [ ] 所有8个YAML对象迁移到TypeScript
- [ ] 对象定义中100%的类型安全
- [ ] 实现所有关键钩子（Account、Contact、Activity、Quote、Case）
- [ ] 零编译错误
- [ ] 文档已更新

### 第二阶段成功标准
- [ ] 实现10+个AI动作
- [ ] 所有主要对象都可使用AI功能
- [ ] AI预测准确率达90%以上（如适用）
- [ ] AI响应时间 < 2秒
- [ ] 用户满意度评分 > 4.5/5

### 第三阶段成功标准
- [ ] 完整的CPQ工作流功能
- [ ] 报价生成时间 < 3秒
- [ ] 服务工单解决时间减少30%
- [ ] 知识库命中率 > 60%
- [ ] 营销活动ROI跟踪运行

### 第四阶段成功标准
- [ ] 完整的RBAC实现
- [ ] 数据安全审计通过
- [ ] 仪表板加载时间 < 1秒
- [ ] 10K记录的报告生成 < 5秒
- [ ] 99.9%正常运行时间SLA

## 🎯 优先级矩阵

### P0 - 关键（优先做）
1. Activity对象迁移
2. Quote对象迁移
3. Case对象迁移
4. Account钩子
5. Opportunity AI增强

### P1 - 高（接下来做）
1. Campaign对象迁移
2. Product/Pricebook迁移
3. Contact钩子
4. Lead AI增强
5. Case AI增强

### P2 - 中（P0/P1之后）
1. Knowledge对象迁移
2. Payment对象迁移
3. Campaign钩子
4. CPQ定价引擎
5. 营销自动化

### P3 - 低（锦上添花）
1. 高级分析
2. 自定义报告
3. 集成连接器
4. 移动优化
5. 多语言UI

## 📅 时间线摘要

| 阶段 | 持续时间 | 关键可交付成果 |
|------|----------|---------------|
| **第一阶段：基础** | 4周 | TypeScript中的所有对象、核心钩子 |
| **第二阶段：AI增强** | 4周 | 跨所有模块的AI动作 |
| **第三阶段：高级功能** | 6周 | CPQ、服务、营销 |
| **第四阶段：企业** | 4周 | 工作流、RBAC、分析 |
| **总计** | **18周** | 世界级CRM系统 |

## 🔄 持续改进

### 代码质量
- 保持90%以上的测试覆盖率
- 每周代码审查
- 自动代码检查和格式化
- 每次提交时进行安全扫描

### 文档
- 为每个主要功能更新README
- 维护API文档
- 为新功能创建用户指南
- 为复杂工作流录制视频教程

### 性能
- 监控查询性能
- 优化数据库索引
- 缓存频繁访问的数据
- 使用类似生产的数据进行负载测试

### 用户反馈
- 每周用户测试会话
- 通过应用内调查收集反馈
- 跟踪功能采用率
- 根据用户需求迭代

## 🚀 开始使用

### 对于开发人员

1. **阅读协议**
   ```bash
   # 查看@objectstack/spec文档
   npm view @objectstack/spec@0.6.1
   ```

2. **设置环境**
   ```bash
   pnpm install
   pnpm build
   pnpm dev
   ```

3. **选择任务**
   - 从P0项目开始
   - 查看GitHub Projects中的分配
   - 创建功能分支：`feature/object-name` 或 `feature/hook-name`

4. **遵循标准**
   - 所有新代码使用TypeScript
   - 遵循命名约定
   - 为新功能编写测试
   - 更新文档

5. **提交PR**
   - 运行代码检查和测试
   - 为UI更改添加截图
   - 向维护者申请审查

### 对于产品经理

1. **优先考虑功能**
   - 每季度审查此计划
   - 根据客户反馈调整优先级
   - 平衡快速见效与长期价值

2. **跟踪进度**
   - 使用GitHub Projects
   - 每周冲刺规划
   - 每月里程碑审查

3. **收集反馈**
   - 用户访谈
   - Beta测试
   - 分析审查

## 📚 参考资料

- [@objectstack/spec v0.6.1文档](https://github.com/objectstack/objectstack)
- [HotCRM架构指南](./README.md)
- [协议升级说明](./UPGRADE_NOTES.md)
- [贡献指南](./CONTRIBUTING.md)

---

**文档版本**: 1.0  
**最后更新**: 2026-01-29  
**作者**: HotCRM开发团队  
**状态**: 活跃开发计划
