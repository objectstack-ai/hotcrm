// Entry point for the server
// In a real ObjectStack app, the CLI handles startup, 
// but this file can serve as a programmatic entry point or custom bootstrap.

// import { run } from '@objectstack/runtime';
import config from './objectstack.config';

console.log('🚀 Starting HotCRM Server...');
// run(config);

