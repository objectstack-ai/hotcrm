---
title: UI 扩展
description: 新增 HotCRM 视图、页面、仪表盘和动作。
---

# UI 扩展

HotCRM UI 元数据位于拥有它的那个包自己的目录下——`src/*/views/`、`src/*/pages/`、`src/*/dashboards/`、`src/*/reports/` 和 `src/*/actions/`，其中 `*` 是四个包之一（`sales`、`service`、`revenue`、`marketing`）。一个条目与它所针对的那个对象放在一起。

## 新增动作

```ts
// src/sales/actions/opportunity.actions.ts
import type { Action } from '@objectstack/spec/ui';

export const MarkOpportunityReviewedAction: Action = {
  name: 'mark_opportunity_reviewed',
  label: 'Mark Reviewed',
  objectName: 'crm_opportunity',
  icon: 'check-circle',
  type: 'modal',
  target: 'mark_opportunity_reviewed',
  locations: ['record_header'],
  body: {
    language: 'js',
    capabilities: ['api.write'],
    source: `
      await ctx.api.object('crm_opportunity').update(input.id, {
        next_step: 'Reviewed by manager'
      });
      return { id: input.id };
    `,
  },
  refreshAfter: true,
};
```

## 新增视图和页面

- 列表和看板定义放在拥有它的那个包的 `views/`——`src/sales/views/`、`src/service/views/`、`src/revenue/views/` 或 `src/marketing/views/`。
- 记录页面和工具页面放在那个包的 `pages/`——今天是 `src/sales/pages/` 或 `src/service/pages/`。
- 从对应的 `index.ts` 导出新元数据。
- 将用户可见标签加入 `src/sales/translations/{locale}.ts`。

## 验证

```bash
pnpm validate
pnpm typecheck
pnpm test
```
