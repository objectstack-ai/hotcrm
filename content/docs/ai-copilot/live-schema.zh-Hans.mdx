---
title: 实时模式（Wow #1）
description: HotCRM 的 AI 在模式变更发生的那一刻就能看到——无需重新训练、无需重启、无需等待。
---

# 实时模式——会自我改写的 CRM

大多数"AI CRM"是在一个静态模式之上外挂一个聊天机器人。一旦管理员添加了一个自定义字段，机器人就会出错：它依据的是上周训练时的模式，而非你今天发布的模式。

HotCRM 与众不同。**模式即系统提示词。**

每次助手回答数据问题时，它都会针对 `@objectstack/runtime` 的元数据服务调用 `describe_object`——该服务会重新读取实时注册表。在 10:00:00 添加一个字段，下一位用户在 10:00:05 提问时就能看到它。

这个助手就是平台的 `ask` 智能体。HotCRM 不自行编写任何智能体——应用只交付**技能（skills）**，技能按 surface 亲和性挂载到平台智能体上（ADR-0063 §1/§2）。下面演示的行为，来自 HotCRM 的 `live_data` 技能搭载于 `ask` 之上。

## 30 秒演示

```bash
# 1. Admin adds a field — no migration, no deploy.
curl -X POST $HOTCRM/api/v1/ai/tools/add_field/invoke \
  -d '{
    "objectName": "crm_account",
    "name": "health_score",
    "label": "Health Score",
    "type": "number"
  }'

# 2. Sales rep asks the assistant — five seconds later.
curl -X POST $HOTCRM/api/v1/ai/chat \
  -d '{
    "agent": "ask",
    "messages": [{ "role": "user",
      "content": "Top 5 accounts by health_score — cite IDs." }]
  }'
```

智能体的工具调用轨迹：

1. `describe_object('crm_account')` → 看到 `health_score`（number）。
2. `query_records('crm_account', orderBy: [{ field: 'health_score', order: 'desc' }], limit: 5)`。
3. 返回带有记录 ID 的排序列表。

总耗时：不到 30 秒。无需重新训练。无需重启。无需部署任何东西。

## 为什么这能成立

| 层级            | 它的作用                                                                   |
|------------------|--------------------------------------------------------------------------------|
| 元数据服务 | 对象 + 字段的单一事实来源。每次写入时热重载。       |
| `describe_object`| 内置 AI 工具。每次调用都重新读取元数据。从不缓存。               |
| `live_data` 技能  | 捆绑 `describe_object` + `query_records`，并促使智能体先行检查。 |
| `ask` 智能体    | 平台的数据智能体。加载该技能，因此回答前一律检查模式，并自动呈现未知字段。 |

运行完整演示：

```bash
./scripts/wow1-live-schema.sh
```

## 这带来了什么

* 字段由运营而非工程添加。
* 每租户的模式扩展，AI 会自动呈现。
* 字段弃用，AI 在它们被移除的那一刻就停止引用。
* 迭代速度以秒计，而非以冲刺周期计。
